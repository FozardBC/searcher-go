package processor

//func TestProc_Save(t *testing.T) {
/*
type fields struct {
		I  *index.Index
		S  *spider.Service
		Db database.IDatabase
	}
	tests := []struct {
		name   string
		fields fields
	}{
		name : "google",
		fields: fields{
			I:
		},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			p := &Proc{
				I:  tt.fields.I,
				S:  tt.fields.S,
				Db: tt.fields.Db,
			}
			p.Save()
		})
	}
}
*/
